name: opendelivery_shared_widgets
description: OpenDelivery shared widgets
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  opendelivery_core:
    path: ../core
  opendelivery_design_system:
    path: ../design_system
  shimmer: ^3.0.0
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
